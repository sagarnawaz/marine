import { notFound } from "next/navigation"; import MarketingPage from "@/components/sections/MarketingPage"; import { blogPosts } from "@/data/phase-one";
export function generateStaticParams(){return blogPosts.map(({slug})=>({slug}));} export default async function Post({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const post=blogPosts.find(p=>p.slug===slug);if(!post)notFound();return <MarketingPage eyebrow={post.category} title={post.title} intro={post.excerpt} cards={[["Start with the facts","Each vessel and instruction has a distinct context. Begin with accurate particulars and a realistic timing plan."],["Prepare early","A tidy set of documents gives authorities and technical reviewers what they need sooner."],["Ask for guidance","Our team can help identify the right route before you commit time or cost."]]} />}
