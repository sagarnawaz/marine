import { notFound } from "next/navigation"; import MarketingPage from "@/components/sections/MarketingPage"; import { industries } from "@/data/phase-one";
export function generateStaticParams(){return industries.map(({slug})=>({slug}));} export default async function IndustryPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const item=industries.find(x=>x.slug===slug);if(!item)notFound();return <MarketingPage eyebrow="Industries we serve" title={item.title} intro={item.summary} cards={[["Focused on your vessel","Advice grounded in your operating needs and timeline."],["One connected team","Registration, technical documents and authorities coordinated together."],["A confident next step","Get a practical scope before work begins."]]} />}
